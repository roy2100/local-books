{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "reader",
  "description": "Capability for reader windows",
  "windows": ["reader-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
